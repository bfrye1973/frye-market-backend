name: dashboard-hourly
on:
  workflow_dispatch: {}
  schedule:
    - cron: "5 * * * *"   # placeholder; won't run due to job guard

jobs:
  build-hourly:
    if: ${{ false }}       # DISABLED (keeps file valid, never runs)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hourly workflow temporarily disabled."
